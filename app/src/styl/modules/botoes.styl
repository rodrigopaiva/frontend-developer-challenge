.btn
	cursor pointer
	background transparent
	border 1px solid transparent
	border-radius 4px
	box-shadow 0 1px 6px 0 rgba(59, 100, 98, 0.16)
	transition .5s ease-out
	outline none
	font_mixin($FontPrimary, 14px, $gray2)
	letter-spacing 1px
	height 40px
	padding 0 15px
	@extend .center-v-h